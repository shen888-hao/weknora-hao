name: Manual Download Helper
on: workflow_dispatch

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: 只有三行，只负责上传
        uses: actions/upload-artifact@v4
        with:
          name: mineru-parts
          path: mineru-cpu.tar.gz.part-* 
